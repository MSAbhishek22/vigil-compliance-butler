name: vigil_flutter
description: Vigil - The High-Stakes Compliance Butler. Your digital guardian for deadlines.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Serverpod client
  serverpod_flutter: ^3.2.0
  
  # Generated client (local reference)
  vigil_client:
    path: ../vigil_client
  
  # UI packages
  cupertino_icons: ^1.0.6
  
  # State management
  provider: ^6.1.1
  
  # Utilities
  intl: ^0.18.1
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  

